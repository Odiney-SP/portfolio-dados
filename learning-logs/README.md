# Learning Logs

Registro do que estou estudando, semana a semana. Um arquivo por mês.

---

## Formato

Não tem regra rígida. O que funciona pra mim:

```markdown
## 2026-05-27

Estudei window functions em SQL hoje — ROW_NUMBER, RANK e LAG.

A diferença entre RANK() e DENSE_RANK() no tratamento de empates fez
sentido depois que pratiquei com um exemplo real de ranking de agentes
por produtividade. LAG() é especialmente útil para comparar com o período
anterior sem precisar de JOIN.

Dúvida que ficou: quando usar ROWS UNBOUNDED PRECEDING vs RANGE?
Próximo: praticar com dataset de produtividade real.
```

Curto. Direto. Sem template elaborado que vai ser abandonado na segunda semana.
